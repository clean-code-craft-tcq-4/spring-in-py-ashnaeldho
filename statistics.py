
def calculateStats(numbers):
  len = len(numbers);
  sum = 0;
  for sum in numbers:
    sum = sum + numbers;
    
  avg = sum / len
  #avg = avg(numbers);
  max = max(numbers);
  min = min(numbers);
  return avg, max, min
