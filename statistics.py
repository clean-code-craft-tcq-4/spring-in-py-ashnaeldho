
def calculateStats(numbers):
  len1 = len(numbers);
  sum = 0;
  for sum in numbers:
    sum = sum + numbers;
    
  avg = sum / len1;
  #avg = avg(numbers);
  max = max(numbers);
  min = min(numbers);
  return avg, max, min
